# Student ID: 0112230016

## Code

```c
#include<stdio.h>
#include<stdlib.h>
#include<string.h>

//node structure
struct Node{
    char name[20];
    int mark;
    struct Node*next;
};

int main(){
struct Node*head=NULL, *temp,*newNode;
 //insert 3 students
  char names [3][20]={"Ayan","Nabila","Farhan"};
  int marks[3]={12 ,15 , 18};
  for (int i=0;i<3;i++){
   newNode=(struct Node*)malloc(sizeof(struct Node));
   strcpy(newNode->name,names[i]);
   newNode->mark=marks[i];
   newNode->next=NULL;

   if(head==NULL){
       head=newNode;
   }
   else{
       temp=head;
       while(temp->next !=NULL)
           temp=temp->next;
           temp->next=newNode;


   }
  }
  //print after insertion

  printf("List after insertion:\n");
  temp=head;
  while(temp!=NULL){
   printf("%s (%d)->",temp->name,temp->mark);
   temp=temp->next;
  }
  printf("NULL\n\n");



//Display the student records

printf("Student records:\n");
   temp=head;
   while(temp !=NULL){
       printf("%s (%d)\n", temp->name,temp->mark);
       temp=temp->next;
   }

   //update 2nd student's marks

   if(head!=NULL && head->next !=NULL){
       struct Node*second=head->next;

       printf("Before:%s (%d)\n", second->name,second->mark);

       second->mark=20;

       printf("After:%s (%d)\n", second->name,second->mark);
       printf("Marks successfully updated.\n");
   }



//delete a student record

if(head!=NULL){
   temp=head;
   head=head->next;
   free (temp);
}

//final list after delete

printf("After delete:\n");
temp=head;
while(temp != NULL){
   printf("%s (%d) ->", temp->name,temp->mark);
   temp=temp->next;
}
printf("NULL\n");

return 0;
}
	







Task 1: Great Work, keep it up


Total Marks: 5 out of 5


Task 2: Looks flawless

Total Marks: 5 out of 5


Task 3: Checked all the cases


Total Marks: 5 out of 5


Task 4: Looks OK


Total Marks: 5 out of 5


Total Marks: 20 out of 20
```
