# Student ID: 0112430738

## Code

```c
#include<stdio.h>
#include<stdlib.h>
struct node
{
       char name[20];
       int data;
       struct node* next;
};


int main()
{
       //1
struct node *a=(struct node*)malloc(sizeof(struct node));
struct node *b=(struct node*)malloc(sizeof(struct node));
struct node *c=(struct node*)malloc(sizeof(struct node));

printf("1st name:");
scanf("%s",a->name);
printf("1st mark:");
scanf("%d",&a->data);
a->next=NULL;

printf("2nd name:");
scanf("%s",b->name);
printf("2nd mark:");
scanf("%d",&b->data);
b->next=NULL;

printf("3rd name:");
scanf("%s",c->name);
printf("3rd mark:");
scanf("%d",&c->data);
c->next=NULL;


//2
printf("%s(%d)->",a->name,a->data);
printf("%s(%d)->",b->name,b->data);
printf("%s(%d)->",c->name,c->data);
printf("NULL\n");

//3
int i=1;
int n=3;
for(i=1;i<n;i++)
{
 if(i==2)
 {
printf("Update mark for 2nd student is:");
scanf("%d",&b->data);
printf("Marks Successfully updated.\n");
 }

}
printf("After Update:\n");
printf("%s(%d)->",a->name,a->data);
printf("%s(%d)->",b->name,b->data);
printf("%s(%d)->",c->name,c->data);
printf("NULL\n");

//4
return 0;
}
	



#include<stdio.h>
#include<stdlib.h>
struct node
{
       char name;
       int data;
       struct node* next;
};

struct node* creatlinklist(arr[],size)
{     struct node *head=NULL,struct node *temp=NULL,struct node *next;
      *temp=(struct node*)malloc(sizeof(struct node));
       int i=0;
       for (i=0;i<size;i++)
       {
               while(head !=0)
                       if (head==0)
               {
                       head=temp;
                     next =temp;

               }
               else
               {
                       next->
               }




       }
}
int main()
{

       return 0;
}
	



#include<stdio.h>
#include<stdlib.h>
struct node
{
       char name[20];
       int data;
       struct node* next;
};


int main()
{
       //1
struct node *a=(struct node*)malloc(sizeof(struct node));
struct node *b=(struct node*)malloc(sizeof(struct node));
struct node *c=(struct node*)malloc(sizeof(struct node));

printf("1st name:");
scanf("%s",a->name);
printf("1st mark:");
scanf("%d",&a->data);
a->next=NULL;

printf("2nd name:");
scanf("%s",b->name);
printf("2nd mark:");
scanf("%d",&b->data);
b->next=NULL;

printf("3rd name:");
scanf("%s",c->name);
printf("3rd mark:");
scanf("%d",&c->data);
c->next=NULL;


//2
printf("%s(%d)->",a->name,a->data);
printf("%s(%d)->",b->name,b->data);
printf("%s(%d)->",c->name,c->data);
printf("NULL\n");

//3
int i=1;
int n=3;
for(i=1;i<n;i++)
{
 if(i==2)
 {
printf("Update mark for 2nd student is:");
scanf("%d",&b->data);
printf("Marks Successfully updated.\n");
 }
}
printf("After Update:\n");
printf("%s(%d)->",a->name,a->data);
printf("%s(%d)->",b->name,b->data);
printf("%s(%d)->",c->name,c->data);
printf("NULL\n");

       return 0;
}
	

Task 1:
1. Smartly Showed the output 
2. However, no connection between linked list
3. The function that supposedly try to connect them is not accurate
4. Main logic is not implemented accurately
5. Your code reminds me of a certain meme


Total Marks: 2.5 out of 5




Task 2:
1. Again, you have shown smartness. 
2. 3 inputs, therefore, 3 print statements work, what will happen with 100 or 2000?
3. Logic is incorrect based on implementation


Total Marks: 2.5 out of 5


Task 3:
1. You are not even looking for the second node.
2. Update logic is ok 


Total Marks: 3 out of 5 




Task 4:
1. I suppose you did not find any clever way to print the delete 


Total Marks: 0 out of 5


Total Marks: 8 out 20
```
