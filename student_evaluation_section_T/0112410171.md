# Student ID: 0112410171

## Code

```c
#include <iostream>
using namespace std;

struct Node {
   string name;
   int marks;
   Node* next;
};

int main() {
   Node *head = NULL, *temp, *newNode;


   string names[3] = {"Ayan", "Nabila", "Farhan"};
   int marksArr[3] = {12, 15, 18};

   for (int i = 0; i < 3; i++) {
       newNode = new Node();
       newNode->name = names[i];
       newNode->marks = marksArr[i];
       newNode->next = NULL;

       if (head == NULL) {
           head = newNode;
       } else {
           temp = head;
           while (temp->next != NULL)
               temp = temp->next;
           temp->next = newNode;
       }
   }

   cout << "Student Records:\n";
   temp = head;
   while (temp != NULL) {
       cout << temp->name << " (" << temp->marks << ")" << endl;
       temp = temp->next;
   }


   temp = head;
   if (temp != NULL && temp->next != NULL) {
       temp = temp->next;
       temp->marks = 20;
       cout << "\nMarks successfully updated.\n";
   }


   cout << "\nUpdated Records:\n";
   temp = head;
   while (temp != NULL) {
       cout << temp->name << " (" << temp->marks << ")" << endl;
       temp = temp->next;
   }

   return 0;
}


	





Task 1:
1. Works seamlessly
2. Great Job


Total Marks: 5 out of 5


Task 2:
1. Printing looks alright


Total Marks: 5 out of 5




Task 3:
1. Update’s condition looks OK
2. Logic is correct


Total Marks: 5 out of 5


Task 4: 
1. Did not attempt


Total Marks: 0 out of 5




N.B.:
1. 100% similarity with ID: 0112410172 
2. Even the zip folder name to file name same
3. Either copied or shared 
4. For this time, I decided not to consult with the Department
5. However, 7 Marks deducted due to participation in cheating 
6. Next time, I will directly inform the department. 


Total Marks: 8 out of 20
```
