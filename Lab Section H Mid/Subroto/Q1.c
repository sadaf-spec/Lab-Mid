#include<stdio.h>
struct Node
{
    char title[30];
    int id;
    struct Node*next;
//TASK 1
}
struct Node*head= Null;

void print_List()
   struct Node*temp=head;
   while(temp !=Null)
   {
       printf("%d,temp>title);


   }
   if(temp->value !=NULL)
   {


    printf("->");
   print("NULL\n");
   }

   //TASK 2
   void display()
   {
       if(head==null);
   }
   void insert_end(char title,int id)
   {
       struct Node *new_item=(struct Node)*malloc(sizef(struct Node));
       new_temp->title=title;
       new_item->value=value;
       new_item->next=NULL;
   }
       if(head==NULL)
       {
        head= new item;
        return;
       }
       struct node*temp=head;
       while(temp->next !=Null);
   {


   temp=temp->next;
   }
   temp_next=new_item;
   {


   void task_4(int value)
   }

   struct node*temp=head;
   head=temp->next;
   free(temp);
   int main()
   {
       insert_end(Sun,10);
       insert_end(Mun,20);
       print_list();

       void update(int value);
       {
           struct
       }

       return 0;

   }

