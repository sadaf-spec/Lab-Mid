# Student ID: 0112510093

## Code

```c
#include<stdio.h>
#include<stdlib.h>
#include<string.h>

struct Node
{
   char movie[50];
   int ID;
   Node* next;
};

node* head = new node;

void linkListTraversal( node* &movie, int ID,char name[50] )
{
   node* curr = new node;
  while (*curr = new node)
  {

  }
}

int main ()
{
  struct node* head = NULL;

  head = insertATend(head, "Inception", (301));
  head = insertATend( head , "Intersteller" , (412));
  head = insertATend( head , "The Dark Knight" , (278));

  linkListTraversal(head);

  displayList( head , movie,ID);

  updateId(head ID);

  remove( head, movie);




   return 0;
}

// task 2

struct node* displayList( struct node* head , char movie[30], int id)
{
   while ( head!= NULL)
   {
       head = head ->next;
   }
   printf()
};;
//task 3



struct node* updateId( struct node* head , int id)
{
if ( head == NULL || head-> next == NULL)
{
    return;
}
head -> next = curr;
};


// task 4
struct node* remove( struct node* head , char movie[] )
{
    head = curr;
    new head = curr->next;
};
	





#include<stdio.h>
#include<stdlib.h>
#include<string.h>

struct Node
{
   char movie[50];
   int ID;
   Node* next;
};

node* head = new node;

void linkListTraversal( node* &movie, int ID,char name[50] )
{
   node* curr = new node;
  while (*curr = new node)
  {

  }
}

int main ()
{
  struct node* head = NULL;

  head = insertATend(head, "Inception", (301));
  head = insertATend( head , "Intersteller" , (412));
  head = insertATend( head , "The Dark Knight" , (278));

  linkListTraversal(head);

  displayList( head , movie,ID);

  updateId(head ID);

  remove( head, movie);




   return 0;
}
// task 1


struct node* insertATend(struct node* head, char movie[],int id)
{
   struct node* ptr=( struct node*)malloc(sizeof(struct node));

   struct node*p = head;
   strcpy(ptr-> movie, movie);
   ptr->id= id;
   ptr-> next=NULL;
};
// task 2

struct node* displayList( struct node* head , char movie[30], int id)
{
   while ( head!= NULL)
   {
       head = head ->next;
   }
   printf()
};;
//task 3



struct node* updateId( struct node* head , int id)
{
if ( head == NULL || head-> next == NULL)
{
    return;
}
head -> next = curr;
};


// task 4
struct node* remove( struct node* head , char movie[] )
{
    head = curr;
    new head = curr->next;
};
	



Task 1:
1. Did not connect the new node with existing list
2. Not traversing to end
3. No output to show


Total Marks: 2 out 5
Task 2:
1. Wrong parameter declaration
2. Print at wrong position
3. Head becomes null
4. Linked List becomes useless after one function call


Total Marks: 2 out of 5


Task 3:
1. Did not update the id with parameter’s id
2. node->data = data needed to be written somewhere


Total Marks: 2 out of 5


Task 4:
1. Wrong syntax
2. Does not remove anything  
3. Head should go to next but need to delete the previous one


Total Marks: 2.5 out 5


Total Marks: 8.5 out 20
```
