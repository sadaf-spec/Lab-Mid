# Student ID: 0112420016

## Code

```c
#include <stdio.h>

struct Node
{
   char title [30];
   int id ;
   int value ;
   struct Node *next ;
};
struct Node *head = NULL ;

void print_List()
{
   struct Node *temp = head ;
   while(temp != NULL)
   {
       printf("%s(%d)", temp->title , temp->value);
       temp = temp->next ;
       if(temp->value != NULL)
       {
           printf(" -> ");
       }
   }

   printf("NULL");
}

void task_2()
{
   struct Node *temp = head ;
   while(temp != NULL)
   {
       printf("%d\n",  temp->value);
       temp = temp->next ;
       if(temp->value != NULL)
       {
           printf(" -> ");
       }
   }

   printf("NULL\n");
}

void insert_end( int value)
{
   struct Node *new_item = (struct Node*)malloc(sizeof(struct Node));
   //new_item -> title = title ;
   new_item -> value = value;
   new_item -> next = NULL;

   if(head == NULL)
   {
       head = new_item ;
       return ;
   }
   struct Node *temp = head ;
   while(temp -> next != NULL)
   {
       temp = temp -> next ;
   }
   temp -> next = new_item ;
}

void task_3(int id , int target)
{
   struct Node *temp = head ;
   while(temp != NULL && temp -> value = target)
   {
       temp = temp -> next ;
   }
   temp -> value = target ;

}


void task_4()
{
   struct Node *temp = head ;
   head = temp -> next ;
   free(temp);
}
void display()
{
   struct Node *temp = head ;
   while(temp != NULL)
   {
       printf("%d",temp->value);
       temp = temp->next ;
       if(temp->value != NULL)
       {
           printf(" -> ");
       }
   }
   printf("NULL\n");

}

int main()
{
   insert_end(301);
   insert_end(412);
   insert_end(278);

   print_List();
   task_4();
   display();

   return 0 ;
}
	





Task 1:
1. I can not think of any logical reason to put two integers in the struct
2. No signs of taking string as an input 


Total Marks: 3 out of 5


Task 2:
1. Temp’s value won’t be null, it is not a pointer.
2. No attempt to print title


Total Marks: 2.5 out of 5


Task 3:
1. Use “==” instead of “=” 
2. Otherwise logic seems correct


Total Marks: 3.5 out of 5


Task 4:
1. Check if the head is null or not.
2. Otherwise, it seems to be correct


Total Marks: 4 out of 5




Total Marks: 13 out of 20
```
