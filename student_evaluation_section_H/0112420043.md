# Student ID: 0112420043

## Code

```c
#include<stdio.h>
struct Node
{
   char title[30];
   int id;
   struct Node* next;
   int temp;
};

struct Node *head = NULL;

void print_List()
{
   while(temp !=NULL){
   printf("%d", temp>title, temp>id);
}
if(temp->value !=NULL)
{
   printf("->");
}
printf("\n");
}
//task 2
void instert_end(char title, int id)
{
   struct Node *new_item = (struct Node *)(sizef(struct Node));

   new_item-> new_title=NULL;
   new_item-> value = value;
   new_item-> next = NULL;


if (head == NULL)
   {
      head = new item;
      return;
   }
   struct Node *temp = head;
   while(temp-> next != NULL){
           temp=temp->next;

   }
   temp-> next = new_item;
   temp_next=new_item;
}
void task_3(int id){
struct Node *temp = head;
while(temp != NULL && temp -> value)
{
   temp = temp -> next;
}
temp->next = next;
}
void task_4(int value){
   struct Node *temp = head;
   head=temp->next;
   free(temp);
}
void display(){
struct Node *temp=head;
while(temp!=NULL){
   printf("%d", temp-> value);
   temp = mtemp->next;
   if(temp->value!=NULL){
       printf("\n");
   }
}}
int main()
{
   insert_end(301);
   insert_end(412);
   insert_end(278);
   print_list();
   print_list();
   task_4();
   void display();
   return 0;
}
	



Task 1:
1. Inserting a single character while initialized an Array 
2. Why did you write temp_next= new_item? Does not make sense
3. head = new_item not head = new item
4. Mostly implementation issues, logic seems correct


Total Marks: 3.5 out of 5


Task 2:
1. Mentioned %d but put two variables instead of one.
2. Did not mention string type for character array.


Total Marks: 3.5 out of 5


Task 3:
1. No Update is being occured
2. Logic does not make sense


Total Marks: 2.5 out of 5


Task 4: 
1. Logic is accurate
2. You could not show me the output of it


Total Marks: 4.5 out of 5


Total Marks: 14 out of 20
ID: 0112510399 
Code:
#include<stdio.h>
#include<stdlib.h>
struct node{
    char title[30];
    int id;
    struct node* next;
};
struct node* head = NULL;
//Task 1
void insertfirst(char title[30];int id;)
{
   struct node* newnode = (struct node*)malloc(sizeof(struct node));
   if(head == NULL){
       head = newnode;
       return ;
   }
   else{
       newnode -> next = head;
       head = newnode;
   }
}
//Task 2
void printlist()
{
   struct node* temp = head;
   while(temp != NULL){
            for(int i =0; i<30;i++){
               printf("%ch (%d)", temp->title[i],temp->id);
            }

       printf("/n");
           temp = temp->next;
   }
}
//Task 3
void updated(int uid)
{
   if(head == NULL){
       printf("nothing to updated");
   }
   head->next->id=uid;
   printf("rental Id updated :");
}
//task 4
void removefirst()
{
   if(head == NULL){
       printf("null");
       return;
   }
   else if(head->next = NULL){
       free (head);
   }
   else{
       struct node* temp = head;
       head= head->next;
       free(temp);
   }
}
int main()
{
   //t1
   insertfirst("inception",301);
   insertfirst("interstaller",412);
   insertfirst("The Dark Knight",278);
   //t2
   printlist("Inception",301);
   //t3
   updated(599);
   //t4
   removefirst();
}
	

Task 1:
1. Task was to insert at the end not insert at the first
2. Only head==NULL case works
Total Marks: 2.5 out of 5


Task 2:
1. Although it will print characters, it would not be the one mentioned in the output test case.
   1. If the name was Samin and id is 21
   2. Printing will look like this: 
      1. S 21
      2. a 21
      3. ….
                It will go on till 30
2. Otherwise, traversing logic is correct


Total Marks: 4 out of 5


Task 3:
1. Logic is correct
2. Output of this part is only visible

Total Marks: 5 out of 5


Task 4:
1. In the second case, you can not free the head directly.
2. Store it to temporary variable
3. Free temporary variable 
4. Make the head null


Total Marks: 4 out of 5


Total Marks: 15.5 out of 20
ID: 0112430236 
Code:
#include<stdio.h>
struct Node
{
   char title[30];
   int id;
   struct Node*next;
//TASK 1
}
struct Node*head= Null;

void print_List()
  struct Node*temp=head;
  while(temp !=Null)
  {
      printf("%d,temp>title);
  }
  if(temp->value !=NULL)
  {

   printf("->");
  print("NULL\n");
  }

  //TASK 2
  void display()
  {
      if(head==null);
  }
  void insert_end(char title,int id)
  {
      struct Node *new_item=(struct Node)*malloc(sizef(struct Node));
      new_temp->title=title;
      new_item->value=value;
      new_item->next=NULL;
  }
      if(head==NULL)
      {
       head= new item;
       return;
      }
      struct node*temp=head;
      while(temp->next !=Null);
  {


  temp=temp->next;
  }
  temp_next=new_item;
  {


  void task_4(int value)
  }

  struct node*temp=head;
  head=temp->next;
  free(temp);
  int main()
  {
      insert_end(Sun,10);
      insert_end(Mun,20);
      print_list();
      void update(int value);
      {
          struct
      }
      return 0;
  }
	

Task 1:
1. Title is a character array but you are taking a parameter for a single character.
2. Did not end the parentheses of the function
3. Otherwise, logic seems correct
4. Do not give any output


Total Marks: 3 out of 5


Task 2:
1. %d to output title is incorrect.
2. The while loop will run till infinity.
3. Temp variable is not moving


Total Marks: 2.5 out of 5


Task 3:
1. No signs of task 3 has found


Total Marks: 0 out of 5


Task 4:
1. Parentheses do not make sense.
2. Code is correct for non empty list
3. Does not check for the empty head.


Total Marks: 3.5 out of 5


Total Marks: 9 out of 20
```
