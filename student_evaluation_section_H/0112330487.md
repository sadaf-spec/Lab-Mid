# Student ID: 0112330487

## Code

```c
#include<stdio.h>
#include<stdlib.h>
typedef struct Node {
   char tittle[30];
   int id;
   struct Node* next_ptr;
}Node;



//Task 1
void insertEnd(char tittle[],int val,Node *head){
   struct Node *new_node = (Node*)malloc(sizeof(Node));
       for(int i=0;i<sizeof(tittle);i++)
       {
          new_node->tittle[i]=tittle[i];
       }

   //new_node->tittle[scan()]=tittle;
   //
   new_node->id = val;
   new_node->next_ptr= NULL;
   ///Case 1 -> Empty
   if(head==NULL){
       head=new_node;
       return;
   }

   ///Case 2
   struct Node *traveler= head;
   
   while(traveler->next_ptr!=NULL){
       traveler = traveler->next_ptr;
   }
   traveler->next_ptr=new_node;
}




//Task 2
void printList(){
   Node *head;
       Node *temp = head;
       while(temp!=NULL){
           printf(" %s (%d)", temp->tittle,temp->id);
           temp=temp->next_ptr;
       }
       printf("NULL\n");
}



//Task3
void updation(int new_id)
{
       Node *head;
       Node *temp = head;
       int count=1;
       while(temp!=NULL){
           temp=temp->next_ptr;
           count++;
           if(count==2)
           {
               temp->id=new_id;
               printf("Rental id Updated\n");
           }
       }


}


//Task4
void delete(Node*head)
{
   if(head==NULL)
   {
       printf("List is already empty.");
       return;
   }
   Node *temp;
   temp=head;
   head=temp->next_ptr;
   free(temp);
}



int main()
{
   Node *head=NULL;
   insertEnd("Inception",301,head);
   insertEnd("Interstellar",412,head);
   insertEnd("the Dark Knight",278,head);
   printList();
   updation(599);
   delete(head);
   printList();
   return 0;
   
}
	

Task 1: 
1. Overall logic correct 
2. Implementation wrong for the string input
3. Does not show any output
Total Marks: 3.5 out of 5


Task 2:
1. Overall Correct
2. No Output shown
Total Marks: 4.5 out of 5


Task 3: 
1. Overall correct 
2. Again due to errors no output shown
Total Marks: 4.5 out of 5


Task 4:
1. Overall Correct
2. Same issue (Can not see output due to errors)
Total Marks:4.5 out of 5


Total: 17 out of 20
```
